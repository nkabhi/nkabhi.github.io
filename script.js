const c=document.getElementById("matrix"),x=c.getContext("2d");function r(){c.width=innerWidth;c.height=innerHeight}r();onresize=r;const cols=()=>Math.floor(c.width/20),drops=[];function init(){drops.length=0;for(let i=0;i<cols();i++)drops[i]=1}init();function draw(){x.fillStyle="rgba(0,0,0,.08)";x.fillRect(0,0,c.width,c.height);x.fillStyle="#0f0";x.font="16px monospace";for(let i=0;i<drops.length;i++){const t=String.fromCharCode(0x30A0+Math.random()*96);x.fillText(t,i*20,drops[i]*20);if(drops[i]*20>c.height&&Math.random()>.975)drops[i]=0;drops[i]++}}setInterval(draw,50);
